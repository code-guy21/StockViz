import React from 'react';
import './SearchBar.scss';

const SearchBar = () => {
	return (
		<>
			<input placeholder='...TSLA' className='searchbox' type='text' />
		</>
	);
};

export default SearchBar;
